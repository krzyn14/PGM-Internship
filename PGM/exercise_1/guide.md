W tym pliku opisane zostało to jak wywołać wykonanie analizy. Z racji, że baza danych została dołączona do repozytorium, 
to można pominąć wykonanie import_csv.py. 

Aby otrzymać xG diff dla obu zespołów w każdym stanie meczu, należy: 
